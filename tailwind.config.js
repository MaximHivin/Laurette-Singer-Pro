module.exports = {
    purge: ['./*.html'],
    // darkMode: false, // or 'media' or 'class'
    theme: {
      extend: {
          colors: {
              gold: "#dac87f",
              red: "#670d0c"
          }

      },
    },
    variants: {},
    plugins: [],
  }